// File contains all of the functions relating to sessions.

/**
 * Creates a new session for the requesting machine.
 * @param key the API key to associate with this session
 * @returns a response object
 */
export function login (key) {

}

/**
 * Deletes the session for the machine the request came from.
 * User must be logged in.
 * @returns a response object
 */
export function logout () {

}

export default {login, logout};